name: Claude Code

# Caller: gọi reusable workflow của org cho chính repo .github này.
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    uses: zsolution-vn/.github/.github/workflows/claude-reusable.yml@main
    secrets: inherit
    # with:
    #   model: claude-opus-4-8
